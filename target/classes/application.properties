# ===================================
# APP NAME
# ===================================
spring.application.name=tse-erp
# ===================================
# SERVER CONFIG
# ===================================
server.port=8080

# ===================================
# DATABASE CONFIG
# ===================================
spring.datasource.url=jdbc:mysql://localhost:3306/tse_erp?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=2360mamun
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===================================
# JPA / HIBERNATE CONFIG
# ===================================
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
# ✅ Warning 1 fix — dialect manually dite hobe na
# spring.jpa.properties.hibernate.dialect — ei line ta thakle remove koro

# ✅ Warning 2 fix — open-in-view disable koro
spring.jpa.open-in-view=false

## Detailed error logging
#logging.level.org.springframework=DEBUG
#logging.level.com.tse.erp=DEBUG

# ===================================
# JWT CONFIG
# ===================================
jwt.secret=tse-erp-super-secret-key-must-be-at-least-256-bits-long-for-hs256-algorithm
jwt.access-token-expiry=900000
jwt.refresh-token-expiry=604800000

# 900000ms   = 15 minutes (access token)
# 604800000ms = 7 days (refresh token)

# ===================================
# SECURITY CONFIG
# ===================================
security.rate-limit.max-attempts-per-ip=5
security.rate-limit.max-attempts-per-account=10
security.rate-limit.window-seconds=10
